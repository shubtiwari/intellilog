function simulateApp() {
  const myError = new Error("Something went wrong with the network");

  const userList = [
    { id: 1, name: "Alice" },
    { id: 2, name: "Bob" },
  ];

  const simpleValue = 42;

  const fetchPromise = Promise.resolve("data");
}
